# Local Variables:
# mode: sh
# End:

# if you modify this, after you're finished, run ~#:updateBash
alias snakes='echo are in the grass'

# Set the timezone to your local timezone 
cd /etc
sudo rm localtime
sudo ln -s /usr/share/zoneinfo/US/Pacific localtime
cd $HOME
